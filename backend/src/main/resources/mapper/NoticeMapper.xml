<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exam.mapper.NoticeMapper">

    <select id="selectNoticePage" resultType="com.exam.entity.Notice">
        SELECT * FROM notice
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY is_top DESC, create_time DESC
    </select>

    <select id="selectPublishedNoticePage" resultType="com.exam.entity.Notice">
        SELECT * FROM notice
        WHERE status = 1
        <if test="type != null and type != ''">
            AND type = #{type}
        </if>
        ORDER BY is_top DESC, create_time DESC
    </select>

    <select id="selectTopNotices" resultType="com.exam.entity.Notice">
        SELECT * FROM notice
        WHERE status = 1 AND is_top = 1
        ORDER BY create_time DESC
        LIMIT 5
    </select>

    <update id="incrementViewCount">
        UPDATE notice SET view_count = view_count + 1 WHERE id = #{noticeId}
    </update>

</mapper>
